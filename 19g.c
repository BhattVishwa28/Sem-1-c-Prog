#include<stdio.h>
char abc(char);

void main ()
{
	int n,d;
	char str[100];
	printf ("Enter n");
	scanf ("%s",str);
	abc(n);
	
}
char abc(char n)
{
	if (ch>='a' && ch<='z'){
		ch=ch-32;
	}
	retutn ch;
}
